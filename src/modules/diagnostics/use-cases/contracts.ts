import type { DoctorResultView, GuildConfigResult, HelpResultView, NoticeView } from '../../ui/view-models.js';

export type AdminNoticeResult = NoticeView;
export type { DoctorResultView, GuildConfigResult, HelpResultView };
